# labs

Isolated experiments. One directory per lab, self-contained, kept forever.

A lab exists to answer **one question** about a technology before that technology touches a real project. It is throwaway code that is deliberately not thrown away: six months later the measured numbers should live next to the code that produced them, not transcribed into prose somewhere else.

## Rules

- **One directory per lab**, named after the thing it answers, not the tech it uses.
- **Every lab has a `README.md`** with: the question, the date it ran, how to run it, what was measured, and the verdict. A lab with no verdict is unfinished.
- **Never commit a key.** Every lab reads credentials from the environment and ships a `.env.example` listing what it needs.
- **Build every lab as a reusable module, not as a throwaway script.** Nothing hardcoded that a consumer would need to change: models, providers, thresholds and prompts are configuration, not literals in an array. Export a clear surface. The point is that after enough labs there is a shelf of working modules covering search, memory, providers and whatever else, ready to be pulled into a real project instead of rewritten.
- **The measurement is still a dated snapshot even when the module is not.** Record the result with the date and versions it was measured under. If the answer changes later, add a new dated result rather than editing the old one, so the history of what was true when stays readable.

## Index

| Lab | Question | Date | Verdict |
|---|---|---|---|
| [`p0-provider-layer`](./p0-provider-layer) | Does the Vercel AI SDK unify genuinely different LLM API shapes with a per-request key, and can a gateway supply the per-model capability data? | 2026-09-08 | **Yes, with one caveat that matters.** The abstraction holds across direct providers and a gateway, and takes a per-request key. OpenRouter's published per-model capabilities predicted real behaviour 5/5, so the capability registry can be read rather than hand-maintained. **But a gateway routes one model across many providers with different capabilities (19 for one model, 5 without tool support), so the routing has to be pinned or tool calling degrades silently.** Round 5 added Vercel AI Gateway as a second gateway (routing pin is a call-time `providerOptions.gateway.only` where OpenRouter pins at model creation, `served-by` exposed unlike OpenRouter, cost $0.00052 for the whole round). **The free tier is rate-limited per model even with a card on file**, which blocked most rows and confounded a clean pinned-vs-unpinned comparison; one unpinned pass already showed two different serving providers inside a single row. Anthropic still untested (rate-limited every attempt). Round 6 (60s spacing, only the 3 rows round 5 left BLOCKED) got `pinned groq` a clean PASS 3/3 matching cerebras's determinism, but `anthropic/claude-haiku-4.5` was still rate-limited on all three checks, so the Anthropic gap remains open. |
| [`p1b-tool-approval`](./p1b-tool-approval) | Does the SDK's native tool approval (human in the loop before a write) work on free small models, and what must the app handle itself? | 2026-08-02 | **Yes for the path that matters:** the loop stops before writing and an approval resumes it into exactly one correct write, on `gpt-oss-120b` and `gpt-oss-20b`, direct on Groq and through OpenRouter alike; **`gemma-4-26b:free` never called the write tool at all**, so a logging feature must name the models it works on. **Three things the app must own:** a denial leaves the model silent so the app words it, a denied history cannot be replayed in a closing call (throws `Tool result is missing`), and small models intermittently emit a malformed tool name, so a write path needs a retry and a clean error. |
| [`p1-tool-loop`](./p1-tool-loop) | Does the AI SDK's native tool loop work reliably on OpenRouter free models with pinned routing, for a 4-tool coach agent? | 2026-08-01 | **Yes, no hand-rolled loop or ReAct fallback justified.** 20/20 measurable cells: correct tool choice, no keyword-baiting, exact arguments, 2-3 step chains completed. gemma-4-26b (darkbloom) 10/10. Only non-quota failure was an upstream infra timeout, retries stay mandatory. **The real ceiling is the free-tier daily quota (~50 req/day): a tool loop spends 2-3 requests per question and the cap died mid-run.** |
| [`p2-memory-supersession`](./p2-memory-supersession) | Do additive `active`/`superseded_by` columns plus deterministic newest-wins fix `coach_facts`'s known gap (a correction never flags an earlier fact as outdated), on the real Gemini embedding pipeline? | 2026-08-05 | **Both tested mechanisms failed, and the failure is the finding.** Similarity-based clustering (mechanism 2) is refuted by measurement: a genuinely distinct pair (0.1754) sits inside the range of real same-topic conflicts (0.1152-0.2056), so no threshold separates the classes. Correction-triggered supersession (mechanism 1) passed the lab but broke on its first real input, because the lab supplied the `correction` category that the real app has an LLM infer, and "no me gusta X, no me lo sugieras más" satisfies the definitions of both `preference` and `correction`. **What shipped is the structured key:** the model names the topic (`subject`), never the relationship, and supersession is an exact string match in code. Verified in the app on a fact pair where neither side was categorized as a correction. |
| [`p3-background-jobs`](./p3-background-jobs) | Which of Vercel Cron, Vercel Workflow DevKit and Inngest fires reliably on a schedule for fit-coach's autonomous maintenance (stale-fact cleanup, memory re-grounding, weekly summaries), inside each provider's free tier, on a throwaway Vercel Hobby deployment? Round 2: does Workflow DevKit give fit-coach's `/api/import/extract` durability worth its cost? | 2026-09-08 | **Plain Vercel Cron for the daily job (round 1, final).** Round 2: **worth adopting for `/api/import/extract` specifically.** Durability, retry/skip semantics and reconnect all confirmed live; cancel flips `Run.status` to `cancelled`, whether in-flight execution stops was not measured; overhead is one extra invocation in the happy path, not per-step fragmentation; needs fit-coach's `proxy.ts` matcher fixed first (`.well-known/workflow/` exclusion) or every run fails in production, confirmed with a real failure. |
| [`p4-shader-page-cost`](./p4-shader-page-cost) | How many kilobytes and how many milliseconds per frame does an animated shader background cost, with vgpu versus three.js, and does either fit a landing page's performance budget? | 2026-09-06 | **Yes, and vgpu is the only one of the three that fits, but the honest budget is ~3x what a headless measurement suggests.** vgpu's page entry is 43.2 KB gzip against three's 125.9 KB (WebGL) and 215.1 KB (WebGPU), confirmed against a real Turbopack chunk (43,982 bytes), so three is disqualified on bundle and vgpu still must be dynamically imported: it spends 86% of a 50 KB hero budget. **The headless round underestimated the browser by ~3x per pixel and that is the finding:** a six-octave fbm hero measured 0.66 ms/Mpx headless but 2.30 ms/Mpx in Chromium on the same machine, because the canvas present/composite path costs ~1.4 ms per frame that an offscreen target never pays. In-browser it is 20.1% of a 60 Hz frame at DPR 1 and 47.2% at DPR 2, so clamping DPR is the only lever that pays and **a headless WebGPU number is a lower bound, not a prediction.** Still Apple Silicon only; integrated graphics and a no-WebGPU fallback remain unmeasured. |
